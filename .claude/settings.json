{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // .tool_response.filePath' | { read -r f; case \"$f\" in */SKILL.md) out=$(hyprlayer orchestrate check \"$f\" --target claude --agents-dir \"${f%/skills/*}/agents\" --agents-dir \"$HOME/.claude/agents\" 2>&1) || { echo \"$out\"; exit 2; };; esac; }",
            "timeout": 30,
            "statusMessage": "Validating orchestration block"
          }
        ]
      }
    ]
  }
}
